apiVersion: 1

providers:
  - name: 'GPU Inference Stack'
    orgId: 1
    folder: 'GPU Inference'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards
      foldersFromFilesStructure: true
